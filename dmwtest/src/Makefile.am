# the program to build (the names of the final binaries)
bin_PROGRAMS = dmwtest

dmwtest_CFLAGS = $(DMW_CFLAGS)
dmwtest_LDADD= $(DMW_LIBS)

# list of sources for the 'dmwbroker' binary
dmwtest_SOURCES =	main.c


